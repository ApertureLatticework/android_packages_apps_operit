二、真正值得看的：9/20 那批没被你们吸收，而且是静默没吸收的
这才是要报的重点。GH 上 14c8afdc^..29d67812 三个提交，改的就是我们树里的同批文件：

14c8afdc feat(draw): modernize SpaceXAI and prompt inputs — 含 xai_draw → spacexai_draw 重命名 + PackageManager 偏好迁移
ef1bf4b6 feat(draw): add image-to-image to bundled image packages — 五个 draw 包全加图生图
29d67812 fix(draw): upload OpenAI URL references as multipart
我逐文件比对了：我们树里的 openai_draw.js / qwen_draw.js / siliconflow_draw.js / zhipu_draw.js / xai_draw.js，与 GH 0f11d962（9/18）逐字节相同。也就是说这三个提交一行都没进来。

为什么是静默的：这条路的历史是 f936608c Drop examples（9/20 01:06，我们侧）把 examples/ 540 个文件砍了 —— 而 build:examples:github + git diff --exit-code -- examples/github.js 那道漂移门禁就在 examples/ 里。门禁随目录一起消失后，上游往 app/src/main/assets/packages/ 推新版就再没有任何东西会报警。单看 git log 完全看不出来。

三、有个坑必须点出来：spacexai_draw 不是重命名，是改名 + 分叉
14c8afdc 干的是：GH 侧删 xai_draw.js，新写 spacexai_draw.js（内容大改，+809 行，加了 grok-imagine-image-2.0 / 1k·2k·4k / 视频线），顺带 xai_draw → spacexai_draw 改名（品牌 = SpaceX + xAI）。

但 xai_draw.js 本来就在你们树里的 packages_whitelist.txt 里 —— 这份名单的消费方是 tools/example_packages/sync_example_packages.py（只干 push 到设备热重载，不是编进 APK）。GH 那边同步改了名单成 spacexai_draw.js。

顺带一个差异：我们 xai_draw.js 的 env 名是 XAI_API_BASE_URL（对得上 37696251 fix(xai-draw): rename env XAI_BASE_URL to XAI_API_BASE_URL 那笔），GH 新版仍是 XAI_API_BASE_URL，这点没漂。
