你的三条核验我全部复验通过，逐条对上。但我顺手把「已发布」这条查实了，结论推翻了我上一条给你的 A/B 选择题。

一、你的核验：三条全过

补两个量化细节：

五包 blob（我们 ≡ 0f11d962）：openai 77015b0e / qwen 032b8081 / siliconflow a9caf49a / zhipu 3371d72d / xai b5409526
spacexai_draw.js 在 29d67812 是 41062 字节，xai_draw.js 在 0f11d962 是 28551 字节 —— 不是同物换壳，差 12.5K
静默机制也复验了：ci/script/pr_check.py 的 ANDROID_FULL_PATTERNS 里没有 examples/**，.github/workflows/ 只把 sync_example_packages.py 列进 paths 触发，没有任何 job 跑 build:examples:github + git diff --exit-code。这段门禁是随 examples/ 一起没的，且 package.json 里连 script 都没有。你说的「无人报警」成立。

env XAI_API_BASE_URL 也复审了：spacexai_draw.js:135 是 getEnv("XAI_API_BASE_URL") || getEnv("XAI_API_BASE_URL") 的双读，没漂。whitelist 行是 xai_draw.js → spacexai_draw.js 单行替换。

二、但「已发布」这条查实了，答案和你想的相反
我拉了 GitHub tag 全集（v1.0.0 … v1.12.2），逐个跑祖先判定：

v1.12.2 contains 14c8afdc: no
v1.12.2 contains ef1bf4b6: no
v1.12.2 contains 29d67812: no
三提交没有任何一个 tag 收（git tag --contains 29d67812 空）。而且时序上根本不可能收：

v1.12.2 = dbf71916，2026-09-18 10:30
14c8afdc / ef1bf4b6 / 29d67812 = 2026-09-15 ~ 09-20 15:16
main 在 9/18 就同步封版了，那三条躺在 dev 上，比最新发布还新。

反过来看 xai_draw：它是已发布接口，而且就在 v1.12.2 里

v1.12.2:app/src/main/assets/packages/xai_draw.js  →  b5409526  ✓ 存在
v1.12.2:app/src/main/assets/packages/spacexai_draw.js  →  不存在
v1.12.2:tools/example_packages/packages_whitelist.txt:26  →  xai_draw.js
再看本仓 app/build.gradle.kts：versionCode = 51 / versionName = "1.12.2"。

也就是说：我们当前这棵树，版本号就是 1.12.2，装着 xai_draw；改名这件事在发布面还完全没发生。

三、所以按你自己的规矩，这里是 A 不是 B
你 AGENTS.md 第 9 条：方案更换先问是否已发布；已发布做向前兼容，未发布就彻彻底底清干净。

现在的情况是两头都占：

发布面（v1.12.2）：xai_draw 是用户确实拿到过的东西 → 存量用户的 enabled/disabled 偏好里躺的是 "xai_draw"
上游面（dev）：改名提交本身未发布
结论：PackageManager.kt 那 37 行偏好迁移必须搬，而且不是可选项。理由不是「要不要兼容上游」，而是我们自己这棵树一旦吸收改名，用户从 1.12.2 升上来就会丢偏好 —— 名字对不上，xai_draw 那条静默失联，用户看到「我明明开着的包没了」。

顺带把迁移块的行为审了一遍（14c8afdc 加的）：

有 renamed_packages_migration_v1 一次性键挡重入 ✓
迁移前先判 RENAMED_PACKAGE_NAMES_V1.values.any { !availablePackages.containsKey(it) } —— 新名没落地就不动偏好，不会把用户数据提前改坏 ✓
同时迁 ENABLED_PACKAGES_KEY 和 DISABLED_PACKAGES_KEY，LinkedHashSet 去重 ✓
这三点设计得对，直上即可，没有要改的。

四、顺手挖到一条你「视野里隐形」的解释
你说 spacexai_draw 在本仓/上游代码搜索 0 命中、只在 provider_xai 显示名里见过 SpaceXAI —— 这条线索是真的、而且同一作者：

6b8fceeb  fix: complete XAI mapping and SpaceXAI branding
          luojiaping <luojiaping@users.noreply.github.com>   2026-08-23
		  14c8afdc 的作者是 3316891527，九月那批改名的作者。品牌叙事在 8 月就由 6b8fceeb 铺好了：显示名改成 SpaceXAI (Grok)、XaiProvider.kt 里 enabledEfforts 改名 supportedEfforts、xai.svg 跟着动。改名 xai_draw → spacexai_draw 是这条品牌线的下游动作，不是孤立事件。
		  
		  而且 6b8fceeb 在 v1.12.2 里（v1.11.0/v1.12.0/v1.12.1 都没有）—— 所以已发布版就已经叫 SpaceXAI 了，只有包名还叫 xai_draw。改名提交要做的是把最后这个不一致收掉。
		  
		  差异化的点：XaiProvider.kt 那笔我们从 f38d10a 带进来了（你查到的那条），所以 Kotlin 品牌侧已对齐、只剩 assets 包名和偏好。
		  
		  五、移植面跟着定稿（按 A，但迁移是必选）
		  件	处置	依据
		  openai/qwen/siliconflow/zhipu 四包	直取 29d67812	blob 与我们的 0f11d962 版不同，是纯功能增量
		  spacexai_draw.js	新增，删 xai_draw.js	改名定稿
		  
ScriptExecutionDialog.kt	直上	我们 hash 56f7d841 ≡ 0f11d962 的同路径 blob，逐字节同，+8/-1 零冲突
PackageManager.kt	手工移植 37 行	我们已分叉（5c00d167 版是我们，430bed15 是上游侧），迁移块纯新增
whitelist	单行替换	已复验
examples/	不接	门禁已不依赖，见上
PackageManager.kt 分叉点我确认了：本仓 hash 430bed1533，0f11d962 是 5c00d167。迁移函数要落在 loadAvailablePackages() 之后那行（上游就是在 706 行附近插的），我这边会按我们的分叉形状手工落。

六、另一条待你拍的（不影响本轮）
ci/README.md、CONTRIBUTING.md

package.json 三处还留着 build:examples:github / git diff --exit-code -- examples/github.js / examples/toolpkg_wasm_demo 的字样，而 examples/ 和对应 script 都没了。这是文档描述已经不存在的门禁，比孤儿字符串更容易误导人（新人照着跑会直接报错）。要不要我一起收口？
